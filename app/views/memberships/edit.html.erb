<!-- Validation failure messages -->
<% if @membership.errors.any? %>
  <% @membership.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Membership #<%= @membership.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_membership/<%= @membership.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for group_id -->
      <div class="form-group">
        <label for="group_id" class="control-label">
          Group
        </label>

        <input type="text" id="group_id" name="group_id" placeholder="Group" class="form-control" value="<%= @membership.group_id %>">
      </div>

      <!-- Label and input for member_id -->
      <div class="form-group">
        <label for="member_id" class="control-label">
          Member
        </label>

        <input type="text" id="member_id" name="member_id" placeholder="Member" class="form-control" value="<%= @membership.member_id %>">
      </div>

      <button class="btn btn-warning">
        Update Membership
      </button>
      or
      <a href="/memberships/<%= @membership.id %>">Cancel</a>
    </form>
  </div>
</div>
